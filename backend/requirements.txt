# Dependências do backend (gerado a partir do que está instalado no venv).
# Manter pinado para reproducibilidade no Docker.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
python-dotenv>=1.0
httpx>=0.27
PyJWT>=2.8
cryptography>=42.0
pydantic>=2.7
# PostgreSQL (usado via Docker; em dev local o SQLite continua funcionando)
psycopg2-binary>=2.9
# Microsoft Graph (SharePoint)
msal>=1.28
# Excel (leitura de planilhas de fiscalização)
openpyxl>=3.1
# Fuso horário
tzdata
